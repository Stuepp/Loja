/*const products = document.querySelectorAll('.product')

    (function shuffle() {
        products.forEach(product => {
            let randomPos = Math.floor(Math.random() * 4)
            product.style.order = randomPos
        })
    })();
*/
function randomPos() {
    let Imgnum = Math.floor(Math.random() * 4)
}